import { DoctorConsentComponent } from './doctor-consent.component';

describe('DoctorConsentComponent', () => {
  it('should create', () => {
    expect(DoctorConsentComponent).toBeTruthy();
  });
});
