info:
  name: Restore Clinic
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url_gateway}}/bff/v1/clinics/:clinicId/restore"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: clinicId
      value: CLN-1
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
