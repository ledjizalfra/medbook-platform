info:
  name: Get My Patient
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url_gateway}}/bff/v1/patients/me"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
