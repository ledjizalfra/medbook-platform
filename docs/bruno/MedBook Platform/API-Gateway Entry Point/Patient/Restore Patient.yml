info:
  name: Restore Patient
  type: http
  seq: 11

http:
  method: PATCH
  url: "{{base_url_gateway}}/bff/v1/patients/:patientId/restore"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: patientId
      value: PAT-1
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
