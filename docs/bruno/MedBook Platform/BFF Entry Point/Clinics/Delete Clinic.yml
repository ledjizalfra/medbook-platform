info:
  name: Delete Clinic
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url_bff}}/bff/v1/clinics/:clinicId"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: clinicId
      value: CLN-1
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
