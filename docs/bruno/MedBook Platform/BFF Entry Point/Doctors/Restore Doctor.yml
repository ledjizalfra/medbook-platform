info:
  name: Restore Doctor
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url_bff}}/bff/v1/doctors/:doctorId/restore"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: doctorId
      value: DOC-1
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
