info:
  name: Delete Doctor
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url_bff}}/bff/v1/doctors/:doctorId"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: doctorId
      value: DOC-1
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
