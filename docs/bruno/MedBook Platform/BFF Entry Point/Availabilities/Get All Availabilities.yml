info:
  name: Get All Availabilities
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url_bff}}/bff/v1/doctors/:doctorId/availabilities"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: doctorId
      value: DOC-1
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
