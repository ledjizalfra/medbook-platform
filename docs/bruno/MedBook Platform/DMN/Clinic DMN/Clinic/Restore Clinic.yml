info:
  name: Restore Clinic
  type: http
  seq: 11

http:
  method: PATCH
  url: "{{base_url_clinic}}/api/v1/clinics/:clinicId/restore"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: clinicId
      value: CLN-1
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
