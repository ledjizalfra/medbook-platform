info:
  name: Patient DMN Health
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url_patient}}/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
