info:
  name: Restore Patient
  type: http
  seq: 12

http:
  method: PATCH
  url: "{{base_url_patient}}/api/v1/patients/:patientId/restore"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: patientId
      value: PAT-7
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
