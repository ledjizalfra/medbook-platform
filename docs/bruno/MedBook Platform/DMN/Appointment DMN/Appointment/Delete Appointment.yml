info:
  name: Delete Appointment
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url_appointment}}/api/v1/appointments/:appointmentId"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: appointmentId
      value: APT-1
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
