info:
  name: Restore Doctor
  type: http
  seq: 11

http:
  method: PATCH
  url: "{{base_url_doctor}}/api/v1/doctors/:doctorId/restore"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: doctorId
      value: DOC-4
      type: path
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
