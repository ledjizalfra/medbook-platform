info:
  name: Eureka Info
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url_eureka}}/actuator/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
