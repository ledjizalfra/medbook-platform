info:
  name: Config-Sever Health
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url_config_server}}/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
