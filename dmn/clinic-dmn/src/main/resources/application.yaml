# =============================================================================
# MedBook Platform — clinic-dmn
# Configurazione di bootstrap — caricata prima del config-server
# =============================================================================

spring:
  application:
    name: clinic-dmn
  profiles:
    active: dev

  config:
    import:
      - "configserver:${CONFIG_SERVER_URL:http://localhost:8071}"
